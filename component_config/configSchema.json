{
  "type": "object",
  "title": "Authentication",
  "properties": {
    "authorization": {
      "type": "object",
      "title": "Authorization",
      "required": ["#api_key"],
      "properties": {
        "#api_key": {
          "type": "string",
          "format": "password",
          "title": "API Token",
          "description": "Your Monday.com API token."
        }
      }
    }
  }
}
